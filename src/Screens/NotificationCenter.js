import React from 'react';
import {View, Text} from 'react-native';

const NotificationCenter = () => {
  return (
    <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
      <Text>NotificationCenter</Text>
    </View>
  );
};

export default NotificationCenter;
